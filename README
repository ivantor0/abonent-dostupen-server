## Серверная сторона проекта Абонент доступен

`v. 1.0`

Чтобы установить:
```
pip install git+https://github.com/user/eve.git
pip install git+https://github.com/user/eve-elastic.git
pip install flask-cors
```

Чтобы запустить:
```
# boot up elasticsearch
python run.py
python flaskserver.py
```
